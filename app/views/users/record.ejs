<!DOCTYPE html>
<html>
	<%- include('./common/head.ejs', { module: "record" }) %>
<body>

	<section class="pt-page-scaleUpCenter chart-box" id="J_container">
		<canvas class="chart" id="J-chart"></canvas>
		<div class="record-list" id="J_recordList"></div>
	</section>

	<div id="J_mask-evalate" class="mask hidden">
		<div class="main slideInUp animated-zing">
			<i class="remove J_mask-close"></i>
			<h3>感谢您对本次消费评价</h3>
			<div clas="input-box">
				<p>您的满意分</p>
				<ul>
					<li class="score-item" data-score="1">1分</li>
					<li class="score-item" data-score="2">2分</li>
					<li class="score-item" data-score="3">3分</li>
					<li class="score-item" data-score="4">4分</li>
					<li class="score-item active" data-score="5">5分</li>
				</ul>
			</div>
			<div clas="input-box">
				<p>您的建议</p>
				<textarea id="evalate-txt" placeholder="您的满意是我们进步的动力"></textarea>
			</div>

			<div class="submit" id="J_submit-evalate">提交评价</div>
		</div>
	</div>

	<script type="text/template" id="recordList-tpl">
		<ul>
		<li class="record-item header">
			<p class="long">消费时间</p>
			<p>消费</p>
			<p class="long">使用优惠券</p>
			<p>餐位</p>
			<p>状态</p>
		</li>
		{@each lists as item}
			<li class="record-item">
				<p class="long"><span>${item.time}</span></p>
				<p><span>¥${item.payMoney}</span></p>
				<p class="long"><span>{@if item.coupon}${item.coupon}{@else}———————{@/if}</span></p>
				<p><span>${item.seat}</span></p>
				<p>
					{@if item.states}
					已评价
					{@else}
					<a href="javascript:void(0)" class="J_btn-evalate">去评价</a>
					{@/if}
				</p>
			</li>
		{@/each}
		</ul>
	</script>

	<!-- chart库 -->
	<script type="text/javascript" src="../../js/lib/chart.min.js"></script>
	<!-- /chart库 -->

	<!-- footer -->
	<%- include('./common/footer-sub.ejs', { module: "record" }) %>
	<!-- /footer -->

</body>
</html>
